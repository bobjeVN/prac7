#include <iostream>
#include <ctime>
#include <string>
#include <windows.h>

using namespace std;

int main() {

    string line;

    ifstream fin("task2.txt");
    while (fin >> line)
        {
            cout << line << endl;
        }

    fin.close();

    return 0;
}
